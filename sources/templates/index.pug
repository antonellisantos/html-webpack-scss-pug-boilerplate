block variables
- var path = self.htmlWebpackPlugin.options
- var pathToFile = (path.mode == 'development') ? "./sources/" : ""

doctype html
html(lang="pt-BR", dir="ltr")

  include ./includes/head.pug
  include ./includes/googleAnalytics.pug
  include ./includes/facebook.pug

  head
    //- +head(path.file)
    //- +googleAnalytics(path.file)

  body
